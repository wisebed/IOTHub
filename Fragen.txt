== ganze Infrastruktur erklären ==
am besten: einmal die ganze Struktur einer Testumgebung mit tatsächlichen Software / Projektnamen (links zu den Projekten) aufmalen. Was wird wirklich gebraucht, was kommt woher, wie hängt es zusammen?

* rest-ws auf wisebed.itm... ist die rest schnittstelle für alle TBs die da eingetragen sind?

== SNAA erklären und Anbindung an iothub als Authentizitätsstelle klären ==
* Wie soll das ablaufen?
** per testbed user/pass ans JS geben, das macht dann request an rest-ws server und setzt cookie?
** js fragt bei iothub restful nach "testbed/uzl/authorize" und iothub fragt direkt beim TB via soap und setzt den cookie. Das ist schlecht, da Fremdcookie wenn rest-ws nicht auf iothub host
*** sollte rest-ws auf iothub host? Warum? Warum nicht?
**

== WiseML ==
* Woher kommt das JSON? (nur https://github.com/wisebed/rest-ws/blob/master/src/main/java/eu/wisebed/restws/util/JSONHelper.java ? Nicht im Standard?)
* Gibt es eine aktuelle Schema? (Ja, schon gefunden)
* wiseml-ruby repository ans ITM übertragen falls gewünscht
