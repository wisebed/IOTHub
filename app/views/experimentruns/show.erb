<h1> Run of <i><%= @run.experiment.name %></i> on <%= @run.testbed.shortname.upcase %></h1>

<p>
  <b>created by:</b>
  <%= link_to @run.user.email, user_path(@run.user) %>
</p>
<p>
  <b>status:</b>
  <%= render :partial => "status", :locals => {:run => @run} %>
</p>

<p>
  <b>files:</b><br>
  <%= render :partial => "files" %>
</p>

<% if current_user %>
    <div id="experiment_run">
      <%= submit_tag 'Run this experiment!', :disable_with => "Please wait...", :id => :experiment_run_button, :class => "btn btn-primary" %>
    </div>
    <% if @run.user == current_user or current_user_is_admin? %>
        <br/>
        <%= link_to "<i class=\"icon-trash\"></i>".html_safe+' Destroy!', @run, confirm: 'Are you sure?', :class => "btn btn-small btn-danger", :method => :delete %>
    <% end %>
<% end %>




